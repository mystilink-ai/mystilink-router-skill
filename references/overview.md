# router — skill reference

Pick **one** primary Mystilink theory skill. The routing table in `SKILL.md` is
the source of truth; do not duplicate it here.

Agent entry: https://www.mystilink.com

Optional Wiki aid:

```text
GET https://wiki.mystilink.com/api/v1/pages/shared.concept.choosing-a-system?locale=en
```

Locale: omit → `en`; missing translations may fall back to `zh-Hans`.
